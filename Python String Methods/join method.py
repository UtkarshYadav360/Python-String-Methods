# join method :
# takes all the items in an iterable and joins them into the string.

# ARGUMENTS :
# iterable : Required. Any iterable object where all the returned values are strings.

my_list = ["Elon", "Jeff", "Mark"]
joined_string = "#".join(my_list)
print(joined_string)
