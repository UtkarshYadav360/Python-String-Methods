# expandtabs method :
# sets the tab size of a string to a specified number of whitespaces.

# ARGUMENTS :
# tabsize : Optional. A number speciyfing the tabsize. Default tabsize is 8.

text = "P\tython"
print(text.expandtabs())
print(text.expandtabs(4))
