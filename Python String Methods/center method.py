# center method :
# center align the string, using a specified character (space is default) as the fill character.

# ARGUMENTS :
# length : length of the string
# character : Optional. Character to fill the missing space on each side.


text = "Python"
print(text.center(20))


new_text = "Python"
print(new_text.center(20, "."))
